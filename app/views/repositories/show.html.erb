<% provide(:title, @repository.name) %>
<% provide(:body_id, 'show_repository') %>

<%= hidden_field_tag(:repository_url, repository_url(@repository)) %>

<div id="psap-repository-page-content">
  <%= render partial: 'show' %>
</div>

<%= render partial: 'shared/edit_panel', locals: { entity_name: 'Repository' } %>
