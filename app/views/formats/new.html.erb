<% provide(:title, 'New Location') %>
<% provide(:body_id, 'new_format') %>

<ol class="breadcrumb">
  <li><%= link_to 'Formats', formats_path %></li>
  <li class="active">New Format</li>
</ol>

<h1>New Format</h1>

<%= form_for(@format) do |f| %>
    <%= render 'form', :f => f %>
    <%= f.submit 'Create Format', class: 'btn btn-primary' %>
<% end %>
