<% provide(:title, 'New Section') %>

<ol class="breadcrumb">
  <li><%= link_to 'Assessment Template', assessments_path %></li>
  <li class="active">New Section</li>
</ol>

<h1>New Section</h1>

<%= form_for(@assessment_section) do |f| %>
    <%= render 'form', :f => f %>

    <%= f.submit 'Create Section', class: 'btn btn-primary' %>
<% end %>
