<% provide(:title, @location.name) %>
<% provide(:body_id, 'show_location') %>

<ol class="breadcrumb">
  <% if current_user.is_admin? %>
    <li><%= link_to 'Institutions', institutions_path %></li>
  <% end %>
  <li>
    <%= link_to @location.repository.institution.name,
                polymorphic_path(@location.repository.institution) %>
  </li>
  <li>
    <%= link_to @location.repository.name,
                polymorphic_path(@location.repository) %>
  </li>
  <li class="active"><%= @location.name %></li>
</ol>

<div class="row">
  <div class="col-sm-4">
    <h1><%= @location.name %> <small>Location</small></h1>
  </div>
  <div class="col-sm-8" style="text-align:right">
    <div class="btn-group">
      <!-- Assess a Resource Here button -->
      <%= link_to new_location_resource_path(@location), class: 'btn btn-default' do %>
          <%= content_tag(:span, '', class: 'glyphicon glyphicon-plus') %>
          Assess a Resource Here
      <% end %>
      <!-- Import button -->
      <div class="btn-group">
        <button type="button" class="btn btn-default dropdown-toggle" data-toggle="dropdown">
          <span class="glyphicon glyphicon-save"></span> Import
          <span class="caret"></span>
        </button>
        <ul class="dropdown-menu">
          <li><%= link_to 'From ArchivesSpace', '',
                          'data-toggle' => 'modal', 'data-target' => '#importModal' %></li>
        </ul>
      </div>
      <!-- Edit -->
      <%= link_to edit_location_path(@location), class: 'btn btn-default' do %>
          <%= content_tag(:span, '', class: 'glyphicon glyphicon-pencil') %>
          Edit
      <% end %>
      <!-- Delete -->
      <%= link_to location_path(@location), method: 'delete',
                  class: 'btn btn-default',
                  data: { confirm: 'Deleting this location will also delete '\
                  'all resource assessments it contains. Are '\
                  'you sure you want to delete this location?' } do %>
          <%= content_tag(:span, '', class: 'glyphicon glyphicon-trash') %>
          Delete
      <% end %>
    </div>
  </div>
</div>

<div class="row">

  <div class="col-sm-8">

    <ul class="nav nav-tabs">
      <li class="active">
        <a href="#resources_tab" data-toggle="tab">Resource Assessments</a>
      </li>
      <li><a href="#activity_tab" data-toggle="tab">Activity</a></li>
    </ul>

    <div class="tab-content">

      <div class="tab-pane active" id="resources_tab">
        <!-- Check/Uncheck All button -->
        <div class="btn-group">
          <button type="button" class="btn btn-default btn-sm psap-check-all">
            Check All
          </button>
          <button type="button" class="btn btn-default btn-sm psap-uncheck-all">
            Uncheck All
          </button>
        </div>

        <!-- Move Checked button -->
        <div class="btn-group" style="float:right">
          <button type="button" class="btn btn-default psap-move-checked"
                  data-toggle="modal" data-target="#moveModal">
            <span class="glyphicon glyphicon-share"></span> Move Checked
          </button>
        </div>

        <%= form_tag(resource_move_path) %>

          <% if @resources.any? %>
            <div id="resources">
              <%= render partial: 'resources/resources',
                         locals: { resources: @resources, show_checkboxes: true } %>
            </div>
          <% else %>
            <p>There are no resources in this location.</p>
          <% end %>

          <!-- Move Checked modal -->
          <div class="modal fade" id="moveModal" tabindex="-1" role="dialog"
               aria-labelledby="moveModalLabel" aria-hidden="true" data-backdrop="false">
            <div class="modal-dialog">
              <div class="modal-content">

                <div class="modal-header">
                  <button type="button" class="close" data-dismiss="modal"><span aria-hidden="true">&times;</span><span class="sr-only">Close</span></button>
                  <h4 class="modal-title" id="moveModalLabel">Move Resources</h4>
                </div>
                <div class="modal-body">
                  <p>Select a location to move the checked resources into:</p>
                  <ul>
                    <% @location.repository.institution.repositories.order(:name).each do |repo| %>
                      <% if repo.locations.any? %>
                        <li><%= repo.name %>
                          <% repo.locations.where('id != ?', @location.id).order(:name).each do |loc| %>
                            <ul>
                              <li><label><input type="radio" name="location_id" value="<%= loc.id %>">
                                <%= loc.name %></label></li>
                            </ul>
                          <% end %>
                        </li>
                      <% end %>
                    <% end %>
                  </ul>
                </div>
                <div class="modal-footer">
                  <button type="button" class="btn btn-default" data-dismiss="modal">Cancel</button>
                  <input type="submit" class="btn btn-primary" value="Move Resources">
                </div>
              </div>
            </div>
          </div>

        </form>

      </div>

      <div class="tab-pane" id="activity_tab">
        <%= render 'shared/events', events: @events %>
      </div>

    </div>

  </div> <!-- .col-sm-8 -->

  <div class="col-sm-4 panel">
    <% if @location.description %>
      <dl>
        <dt>Description</dt>
        <dd><%= @location.description %></dd>
      </dl>
    <% end %>
  </div>

</div> <!-- .row -->

<!-- Import From ArchivesSpace modal -->
<div class="modal fade" id="importModal" tabindex="-1" role="dialog"
     aria-labelledby="importModalLabel" aria-hidden="true">
  <div class="modal-dialog">
    <div class="modal-content">
      <%= form_for(@import, url: location_resource_import_post_path(@location)) do |f| %>
        <div class="modal-header">
          <button type="button" class="close" data-dismiss="modal" aria-hidden="true">&times;</button>
          <h4 class="modal-title" id="importModalLabel">Import a Resource From ArchivesSpace</h4>
        </div>
        <div class="modal-body">
          <%= render 'shared/error_messages', :object => f.object %>

          <div class="form-group">
            <%= f.label :host, 'ArchivesSpace Host' %>
            <%= f.text_field :host, class: 'form-control' %>
          </div>

          <div class="form-group">
            <%= f.label :port, 'ArchivesSpace Port' %>
            <%= f.number_field :port, min: 0, max: 65536, maxlength: 5,
                               class: 'form-control' %>
          </div>

          <div class="form-group">
            <%= f.label :username, 'ArchivesSpace Username' %>
            <%= f.text_field :username, class: 'form-control' %>
          </div>

          <div class="form-group">
            <%= f.label :password, 'ArchivesSpace Password' %>
            <%= f.password_field :password, class: 'form-control' %>
          </div>

          <div class="form-group">
            <%= f.label :resource_id, 'ArchivesSpace Resource ID' %>
            <%= f.text_field :resource_id, class: 'form-control' %>
          </div>
        </div>
        <div class="modal-footer">
          <button type="button" class="btn btn-default" data-dismiss="modal">Cancel</button>
          <%= f.submit 'Import', class: 'btn btn-primary' %>
        </div>
      <% end %>
    </div>
  </div>
</div>
