<%= form_for(@location, remote: true) do |f| %>

  <div class="psap-error-messages">
    <% # populated via ajax by _show_error.js.erb
    %>
  </div>

  <p>In most cases, location will be determined by
    architectural divisions, such as buildings, floors, or rooms, or other
    areas with discrete environmental conditions or security parameters. If
    further granularity in locations is desired, a consistent naming
    convention from largest to smallest entity separated by semicolons is
    suggested. For instance: "First Floor; Room 223; West Wall, Shelving Unit
    2".</p>

  <hr>

  <div class="form-group">
    <%= f.label :name, 'Name', class: 'required' %>
    <%= f.text_field :name, class: 'form-control' %>
  </div>

  <div class="form-group">
    <%= f.label :description %>
    <%= f.text_area :description, class: 'form-control' %>
  </div>

  <%= hidden_field_tag(:repository_id, @location.repository.id) %>

  <button type="button" class="btn btn-default" data-dismiss="modal">Cancel</button>
  <%= f.submit(action == :create ? 'Create Location' : 'Save Changes',
               class: 'btn btn-primary') %>

<% end %>
