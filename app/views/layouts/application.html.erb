<!DOCTYPE html>
<html>
<head>
  <title>Preservation Self-Assessment Program (PSAP) | <%= yield(:title) %></title>
  <meta charset="utf-8">
  <meta http-equiv="X-UA-Compatible" content="IE=edge">
  <meta name="viewport" content="width=device-width, initial-scale=1">
  <meta name="request-method" content="<%= request.method %>">

  <%= csrf_meta_tags %>
  <%= favicon_link_tag 'favicon.ico' %>

  <% # https://developer.apple.com/library/ios/documentation/AppleApplications/Reference/SafariWebContent/ConfiguringWebApplications/ConfiguringWebApplications.html %>
  <%= favicon_link_tag 'touch-icon-60.png', rel: 'apple-touch-icon',
                       type: 'image/png' %>
  <%= favicon_link_tag 'touch-icon-60@2x.png', rel: 'apple-touch-icon',
                       sizes: '120x120', type: 'image/png' %>
  <%= favicon_link_tag 'touch-icon-76.png', rel: 'apple-touch-icon',
                       sizes: '76x76', type: 'image/png' %>
  <%= favicon_link_tag 'touch-icon-76@2x.png', rel: 'apple-touch-icon',
                       sizes: '152x152', type: 'image/png' %>

  <%= stylesheet_link_tag 'application', media: 'all' %>
  <!--[if lt IE 9]>
  <script src="https://raw.githubusercontent.com/aFarkas/html5shiv/master/src/html5shiv.js"></script>
  <![endif]-->

  <%= javascript_include_tag 'application', 'data-turbolinks-track' => true %>
</head>
<body <%= !yield(:body_id).empty? ? raw("id=\"#{yield(:body_id)}\"") : nil %>
      data-spy="scroll" lang="en-US">

  <nav class="navbar navbar-default navbar-inverse navbar-fixed-top" role="navigation">

    <div class="navbar-header">
      <button type="button" class="navbar-toggle" data-toggle="collapse" data-target="#bs-example-navbar-collapse-1">
        <span class="sr-only">Toggle navigation</span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
      </button>
    </div>

    <div class="container-fluid">

      <div class="collapse navbar-collapse" id="bs-example-navbar-collapse-1">

        <ul class="nav navbar-nav navbar-left">
          <% if signed_in? %>
            <% if current_user.institution %>
              <li class="dropdown <%= 'active' if current_page?(dashboard_path) or
                      controller.controller_name == 'institutions' %>">
                <a href="#" class="dropdown-toggle nav-link" data-toggle="dropdown">
                  <i class="fa fa-university"></i> My Institution <b class="caret"></b>
                </a>
                <ul class="dropdown-menu">
                  <li><%= link_to 'Home', current_user.institution %></li>
                  <li role="presentation" class="divider"></li>
                  <li><%= link_to 'Repositories', institution_repositories_path(current_user.institution) %></li>
                  <li><%= link_to 'Resource Search', institution_resources_path(current_user.institution) %></li>
                  <li><%= link_to 'Assessment Report', institution_assessment_report_path(current_user.institution) %></li>
                  <li><%= link_to 'Users', institution_users_path(current_user.institution) %></li>
                  <li><%= link_to 'Activity', institution_events_path(current_user.institution) %></li>
                </ul>
              </li>
            <% else %>
              <li <%= 'class=active' if current_page?(dashboard_path) %>>
                <%= link_to 'Home', dashboard_path, class: 'nav-link' %>
              </li>
            <% end %>
          <% end %>

          <li class="dropdown <%= 'active' if controller.controller_name == 'format_id_guide' %>">
            <a href="#" class="dropdown-toggle nav-link" data-toggle="dropdown">
              Format ID Guide <b class="caret"></b>
            </a>
            <ul class="dropdown-menu">
              <li><%= link_to 'All Formats', format_id_guide_path %></li>
              <li class="divider"></li>
              <li><%= link_to 'Audiovisual Media', format_id_guide_path(anchor: 'audiovisual') %></li>
              <li><%= link_to 'Paper & Book', format_id_guide_path(anchor: 'paper_book') %></li>
              <li><%= link_to 'Photo & Image Material', format_id_guide_path(anchor: 'photo_image') %></li>
              <li><%= link_to 'Supplementary', format_id_guide_path(anchor: 'supplementary') %></li>
            </ul>
          </li>
          <li <%= 'class=active' if current_page?(glossary_path) %>>
            <%= link_to 'Glossary', glossary_path, class: 'nav-link' %>
          </li>
          <li <%= 'class=active' if current_page?(bibliography_path) %>>
            <%= link_to 'Bibliography', bibliography_path, class: 'nav-link' %>
          </li>
        </ul>

        <ul class="nav navbar-nav navbar-right">
          <% if signed_in? %>
            <% if current_user.is_admin? %>
              <li class="dropdown">
                <a href="#" class="dropdown-toggle nav-link" data-toggle="dropdown">
                  <i class="fa fa-cog" aria-hidden="true"></i>
                  Administration <b class="caret"></b>
                </a>
                <ul class="dropdown-menu">
                  <li><%= link_to 'Institutions', institutions_path %></li>
                  <li><%= link_to 'Formats', formats_path %></li>
                  <li><%= link_to 'Users', users_path %></li>
                  <li class="divider"></li>
                  <li><%= link_to 'Event Log', events_path %></li>
                </ul>
              </li>
            <% end %>

            <li class="dropdown">
              <a href="#" class="dropdown-toggle nav-link" data-toggle="dropdown">
                <i class="fa fa-user" aria-hidden="true"></i>
                <%= current_user.username %> <b class="caret"></b>
              </a>
              <ul class="dropdown-menu">
                <li><%= link_to 'Profile', current_user %></li>
                <li><%= link_to 'Settings', edit_user_path(current_user) %></li>
                <li class="divider"></li>
                <li>
                  <%= link_to 'Sign Out', signout_path, method: 'delete' %>
                </li>
              </ul>
            </li>

            <li class="psap-icon-only">
              <%= link_to(user_manual_path, title: 'User Manual') do %>
                <i class="fa fa-book"></i>
              <% end %>
            </li>
            <li class="psap-icon-only">
              <%= link_to(help_path, title: 'Help') do %>
                <i class="fa fa-question-circle"></i>
              <% end %>
            </li>
          <% elsif request.url != root_url %>
            <li><%= link_to 'Sign in', signin_path, class: 'nav-link' %></li>
          <% end %>
        </ul>
      </div>
    </div>
  </nav>

  <div id="psap-ajax-shade"></div>

  <div class="container-fluid">
    <header class="page-header">
      <%= link_to retina_image_tag('psap_logo.png', size:'156x70',
                                   alt:'PSAP logo', id:'title_logo'), root_path %>
      <div class="btn-group">
        <% if signed_in? and current_user.institution %>
          <button type="button" class="btn btn-default btn-sm"
                  data-container="body" data-toggle="popover"
                  data-placement="bottom" data-content="<p>Before you can assess
                  an item, you must first add it to a location. Resources in the
                  PSAP are structured like this:</p>

                  <ul class=&quot;psap-header-assessment-help&quot;>
                    <li><%= escape_once(entity_icon(Institution)) %> Institution
                      <ul>
                        <li><%= escape_once(entity_icon(Repository)) %>Repository
                          <ul>
                            <li><%= escape_once(entity_icon(Location)) %>Location
                              <ul>
                                <li><%= escape_once(entity_icon(Resource)) %>Resource (Item or
                                Collection)</li>
                              </ul>
                            </li>
                          </ul>
                        </li>
                      </ul>
                    </li>
                  </ul>

                  <p>To create an item, navigate to the location you want it to
                   reside in, and then click the &quot;Add Resource&quot; button.
                   Once it has been added, it is ready to assess.">
            <span class="glyphicon glyphicon-stats"></span> Assess an Item
          </button>
          <%= link_to institution_assessment_report_path(current_user.institution),
                      class: 'btn btn-default btn-sm' do %>
            <span class="glyphicon glyphicon-stats"></span> Assessment Report
          <% end %>
        <% end %>
        <%= link_to 'mailto:nobody@example.org?subject=I found a bug in '\
        'the PSAP&body=Please enter a description of the bug, the URL of the '\
        'page on which it occurs, and instructions on how to reproduce it. Thanks!',
                    class: 'btn btn-default btn-sm psap-report-bug' do %>
          <span class="fa fa-bug"></span> Report a Bug
          <script type="text/javascript">
              var anchor = $('.psap-report-bug');
              var href = anchor.attr('href');
              var addr = 'psap';
              addr += '@library.illinois.edu';
              anchor.attr('href', href.replace('nobody@example.org', addr));
          </script>
        <% end %>
      </div>
      <h1><%= link_to 'Preservation Self-Assessment Program', root_path, class: 'psap-title' %>
        <span class="label label-warning">BETA</span></h1>
    </header>

    <% flash.each do |type, message| %>
      <div class="alert alert-dismissable psap-flash <%= bootstrap_class_for_alert(type) %>">
        <button type="button" class="close" data-dismiss="alert"
                aria-hidden="true">&times;</button>
        <%= message %>
      </div>
    <% end %>

    <div style="clear:both"></div>

    <%= render 'layouts/modal_panel' %>

    <div id="psap-page-content">
      <%= yield %>
    </div>

    <%= render 'layouts/footer' %>

  </div>

  <% if Rails.env.production? %>
    <!-- Google Analytics -->
    <script>
        (function(i,s,o,g,r,a,m){i['GoogleAnalyticsObject']=r;i[r]=i[r]||function(){
            (i[r].q=i[r].q||[]).push(arguments)},i[r].l=1*new Date();a=s.createElement(o),
                m=s.getElementsByTagName(o)[0];a.async=1;a.src=g;m.parentNode.insertBefore(a,m)
        })(window,document,'script','//www.google-analytics.com/analytics.js','ga');

        ga('create', 'UA-56715538-1', 'auto');
        ga('send', 'pageview');
    </script>
  <% end %>

  </body>
</html>
