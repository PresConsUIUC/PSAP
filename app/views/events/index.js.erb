$('#events_list').html('<%= escape_javascript(render('events')) %>');
$('.pagination a').attr('data-remote', 'true');

PSAP.updateResultsCount();

$('input[name="level"]').val(<%= params[:level] %>);
